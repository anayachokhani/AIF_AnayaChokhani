# Raw Data

Create these local directories when downloading ABO:

```bash
mkdir -p data/raw/abo data/external/abo
```

Expected files:

- `data/raw/abo/abo-listings.tar`
- optional: `data/raw/abo/abo-images-small.tar`
- `data/raw/abo/SOURCE.txt`

Raw and extracted data are ignored by Git.
